import type { Store } from '../storeTypes.js';

/**
 * Hand-verified store seed data.
 *
 * Keep this list empty until coordinates are verified from reliable sources.
 * Do not add guessed coordinates. Wrong distance is worse than no distance.
 */
export const seedStores: Store[] = [];
